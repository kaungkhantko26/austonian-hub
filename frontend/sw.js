const CACHE='austonian-hub-v56';
const ASSETS=['/','/index.html','/styles.css','/desktop.css','/app.js','/manifest.webmanifest','/assets/auston-logo.png','/assets/app-icon-192.png','/assets/app-icon-512.png','/assets/apple-touch-icon.png','/assets/favicon-32.png','/assets/home.svg','/assets/card.svg','/assets/jobs.svg','/assets/class.svg','/assets/feed.svg','/assets/settings.svg','/assets/admin.svg','/assets/bell.svg','/assets/language.svg','/assets/appearance.svg','/assets/privacy.svg','/assets/support.svg','/assets/benefit-star.svg','/assets/benefit-sport.svg','/assets/benefit-phone.svg','/assets/benefit-snack.svg'];
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(ASSETS)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{const path=new URL(event.request.url).pathname;if(event.request.method==='GET'&&!path.startsWith('/api/')&&!path.startsWith('/supabase-'))event.respondWith(fetch(event.request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy));return response}).catch(()=>caches.match(event.request)))})
self.addEventListener('notificationclick',event=>{event.notification.close();const destination=event.notification.data?.url||'/#app';event.waitUntil(self.clients.matchAll({type:'window',includeUncontrolled:true}).then(clients=>{const existing=clients.find(client=>'focus' in client);if(existing){existing.navigate(destination);return existing.focus()}return self.clients.openWindow(destination)}))});
self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={title:'Austonian Hub',body:event.data?event.data.text():''}}const title=data.title||'Austonian Hub',options={body:data.body||'',icon:'/assets/app-icon-192.png',badge:'/assets/favicon-32.png',tag:data.tag||'austonian-update',data:{url:data.url||'/#app'},vibrate:[80,40,80]};event.waitUntil(self.registration.showNotification(title,options))});
